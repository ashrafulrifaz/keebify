import CheckoutSteps from "@/components/CartComponents/CheckoutSteps";

const page = () => {
    return (
        <div>
            <CheckoutSteps step={1} />
        </div>
    );
};

export default page;